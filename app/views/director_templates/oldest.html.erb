<h1>Oldest director</h1>

<%oldest_director = Director.where.not({ :dob => nil}).all.order({:dob => :asc}).at(0)%>

<p>The oldest director in our dataset right now is <a href="/directors/<%=oldest_director.id%>"><%=oldest_director.name%></a>, born on <%=oldest_director.dob%>.</p>
